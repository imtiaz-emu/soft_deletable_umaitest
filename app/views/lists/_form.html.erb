<div aria-hidden="true" aria-labelledby="myModalLabel" role="dialog" tabindex="-1" id="listForm" class="modal fade" style="display: none;">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button aria-hidden="true" data-dismiss="modal" class="close" type="button">×</button>
        <h4 class="modal-title">
          <%= list.new_record? ? 'Create List' : 'Edit List' %>
        </h4>
      </div>
      <div class="modal-body">
        <%= form_for(list, remote: true) do |f| %>
            <div class="form-group">
              <label class="sr-only" for="exampleInputEmail2">List Name</label>
              <%= f.text_field :title, class: "form-control ", id: "exampleInputEmail5", placeholder: "List Name", required: true %>
            </div>

            <div class="form-group">
              <label class="sr-only" for="exampleInputEmail2">List Description</label>
              <%= f.text_area :description, rows: '4', class: 'form-control', id: 'inputDesc', placeholder: "List description" %>
            </div>
            <button type="submit" class="btn btn-success"><%= list.new_record? ? 'Create' : 'Update' %></button>
        <% end %>

      </div>

    </div>
  </div>
</div>